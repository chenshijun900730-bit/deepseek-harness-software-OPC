# SPRINT_CONTRACT — TASK-20260815-001 / S01

- 冻结时间：2026-08-15T09:15:39.302Z
- 签署角色：coordinator
- 任务类型：small
- 确定性门禁（小型）：true（true）
- 计划末轮：是
- 合同校验值：880bb170

> 冻结后合同不可修改；Generator 不得修改验收标准来迎合实现；Evaluator 不得顺手修代码。

## 目标
将演示应用首页 id=submit 提交按钮背景色由灰色改为蓝色 #1e40af（rgb(30,64,175)）；位置、尺寸、可点击状态与提交行为保持不变。

## 非目标
不改按钮位置/尺寸/文案/其他样式；不改提交逻辑、表单校验、数据流；不改首页以外页面；不引入依赖。

## 用户操作
1. 打开演示应用首页，定位 id=submit 按钮。

## 可观察结果
1. 按钮计算样式 background-color == rgb(30,64,175)。
2. 未提交状态下按钮可点击（disabled=false），点击后提交行为与改色前一致。
3. 按钮几何位置与尺寸与改色前一致（无布局位移）。
4. 首页其他按钮颜色未改变。

## 技术证据（确定性自动测试，覆盖全部验收标准）
- TEST-COLOR-01：Playwright 断言计算样式颜色值 == rgb(30,64,175)。
- TEST-CLICK-01：断言可点击、提交调用次数与改色前一致。
- TEST-GEOM-01：断言 offsetLeft/Top/Width/Height 与基线一致。

## 必测项
TEST-COLOR-01、TEST-CLICK-01、TEST-GEOM-01。

## 失败定义
任一断言不通过 → FAIL；测试无法自动判定 → 提升为中型任务。

## 文件所有权
generator 独占：demo-app/ 下首页按钮相关文件。
