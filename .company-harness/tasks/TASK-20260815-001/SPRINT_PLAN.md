# SPRINT_PLAN — TASK-20260815-001

> 负责人：Planner。Sprint 路线、每轮目标、依赖顺序。

## Sprint 路线

| Sprint | 目标 | 可观察完成标准 | 依赖 |
| --- | --- | --- | --- |
| S01 | 将首页 id=submit 提交按钮背景色由灰色改为蓝色 #1e40af，同时保持按钮位置、尺寸、可点击状态与提交行为不变 | 1) 首页 id=submit 按钮可见，计算样式 background-color 为 #1e40af（rgb(30,64,175)）；2) 未提交时按钮可点击，点击后提交行为与改色前一致；3) 按钮位置与尺寸较改色前无变化；4) 其他按钮颜色与首页以外页面无回归 | 无外部依赖；需可运行的演示应用及能读取计算样式、执行点击的验证手段（浏览器 DevTools 或现有测试框架） |

## 说明

- 每轮一个可控功能块；完成标准在编码前冻结。
- 小型任务仍使用标准 SPRINT_CONTRACT.md，但由 Coordinator 依据原始需求与现有确定性测试冻结。